#include <iostream>

using namespace std;

int main()
{
	setlocale(0, "Russian");
	int a,b,h;
	double f;
	cin >> a >> b >> h;
	cout << "арг-т\t" << "функция\n";
	while (a <= b){
		f = sin(a) * sin(a);
		cout << a << "\t" << f << endl;
		a = a + h;
	}

return 0;
}
